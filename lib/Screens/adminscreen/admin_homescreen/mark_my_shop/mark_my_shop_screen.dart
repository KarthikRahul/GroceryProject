import 'package:flutter/material.dart';

class MarkMyShopScreen extends StatelessWidget {
  const MarkMyShopScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Mark My Shop")),
    );
  }
}
