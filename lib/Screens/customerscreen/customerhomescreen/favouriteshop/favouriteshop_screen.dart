import 'package:flutter/material.dart';

class FavouriteShopScreen extends StatelessWidget {
  const FavouriteShopScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Favorite shop")),
    );
  }
}
