import 'package:flutter/material.dart';

class CustomersProfileScreen extends StatelessWidget {
  const CustomersProfileScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("customer profile screen")),
    );
  }
}
